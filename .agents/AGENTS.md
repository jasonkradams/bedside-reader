## Custom Rules

- Any fixes or changes must come with a regression test first (TDD style). First create the failing test (red), run the test to enure it fails, then write the code to resolve it (green), ensure the test passes, and then push the code.
